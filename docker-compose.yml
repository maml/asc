services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: asc
      POSTGRES_USER: asc
      POSTGRES_PASSWORD: asc_dev_password
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
